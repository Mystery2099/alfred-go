services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
      DATABASE_URL: /data/alfredgo.sqlite
    volumes:
      - alfredgo-data:/data

volumes:
  alfredgo-data:
